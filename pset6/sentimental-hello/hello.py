# First prompt user to input their name, assign the response to variable "name".
name = input("What is your name?")

# Print out greeting "Hello, Name" using f-string syntax to interpolate the value of "name" into the string.
print(f"Hello, {name}")